
#include <stdlib.h>
#include <stdio.h>
#include <time.h>

/**
 * main - enrty point
 * Return: Always 0 (Seccess)
 */
int main(void)

{
	char low;

for (low = 'z'; low >= 'a'; low--)
putchar(low);
putchar('\n');
return (0);
}
