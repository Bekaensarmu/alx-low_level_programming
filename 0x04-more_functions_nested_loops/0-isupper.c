#include "main.h"

/**
 * _isupper - check if the letter is upper
 * @x: The to be checked
 * Return: 1 for upper letter 0 for any else
 */

int _isupper(int x)
{
if (x >= 65 && x <= 98)
{
return (1);
}
return (0);
}
